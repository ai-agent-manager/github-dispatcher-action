{
  "name": "@ai-agent-manager/github-dispatcher-action",
  "version": "1.3.1",
  "description": "GitHub Action that dispatches AI agent skills against pull request events.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "prepare": "git rev-parse --git-dir >/dev/null 2>&1 && git config core.hooksPath .githooks || true",
    "test": "tsx --test test/*.test.ts",
    "lint": "eslint src test",
    "lint:fix": "eslint src test --fix",
    "typecheck": "tsc --noEmit",
    "validate": "npm run typecheck && npm test && npm run build && npm run lint",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "js-yaml": "^5.2.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^26.2.0",
    "@vercel/ncc": "^0.44.1",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0"
  },
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ai-agent-manager/github-dispatcher-action"
  },
  "license": "Apache-2.0"
}
